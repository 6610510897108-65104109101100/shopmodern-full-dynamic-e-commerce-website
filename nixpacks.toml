[phases.setup]
nixPkgs = ["php82", "nodejs_22"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install"
]

[phases.build]
cmds = [
  "npm run build",
  "php artisan optimize:clear",
  "php artisan config:cache",
  "php artisan route:cache",
  "php artisan view:cache"
]

[start]
cmd = "chmod -R 775 storage bootstrap/cache && php artisan serve --host=0.0.0.0 --port=$PORT"
